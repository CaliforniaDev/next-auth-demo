import { passwordResetTokensSchema } from './passwordResetTokensSchema';
// To push the schema to the database, run the following command in the terminal:
//*: npx drizzle-kit push 


export { users } from './usersSchema';
export { passwordResetTokensSchema } from './passwordResetTokensSchema';